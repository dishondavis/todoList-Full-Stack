<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="styles.css">
    <title></title>
  </head>
  <body>
    <h3>To Do List</h3>
    <h4>You have <%= unfinished %> tasks left!</h4>



    <ul>
    <% for(var i=0; i<todos.length; i++) {%>
      <li class= "todoItem">
        <% if(todos[i].isComplete === true){ %>
          <span class="crossOut" data-name="<%= todos[i].name %>"><%= todos[i].isComplete %></span>
          <% } else { %>
          <span class="todos" data-name="<%= todos[i].name %>"><%= todos[i].name %></span>
          <% } %>
        <button class="throwAway" type="button" name="Trash" placeholder="Trash">Trash</button>
      </li>
    <% } %>
    </ul>




    <h2>Add To Do Item</h2>
    <form action="/todos" method="POST">
      <input type="text" placeholder="name" name="name">
      <button type="submit">Submit</button>
    </form>

    <script src="main.js"></script>
  </body>
</html>
